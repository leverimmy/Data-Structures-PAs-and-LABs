#include <bits/stdc++.h>
#include <windows.h>

int main() {

    while (1) {
        system("gen.exe");
        system("std.exe");
        system("match.exe");
        if (system("fc oup.out ans.out"))
            break;
    }

    return 0;
}